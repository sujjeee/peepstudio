type ComponentData = {
    code: string;
    component: JSX.Element;
    name: string;
};

interface PeepData {
    edit: boolean
    refresh: boolean
    skinColor: string;
    hairColor: string;
    beardColor: string;
    frameColor: string;
    head: string;
    body: string;
    face: string;
    beard: string;
    mask: string;
    accessory: string;
}